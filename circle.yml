dependencies:
  pre:
    - ./circleci.sh install-deps
  cache_directories:
    - "~/dlang"

test:
  override:
    - ./circleci.sh setup-repos
    - ./circleci.sh style
    - ./circleci.sh publictests
    - ./circleci.sh coverage:
        parallel: true
        timeout: 1200

  post:
    - ./circleci.sh codecov
